// TODO: pick run/seed with highest surprise
// - then see if can branch off such that:
//   - one branch keeps original result
//   - the other branch keeps surprise
// - do this recursively
//   - if combined result better at any time, then keep
// - generalization of PassThrough and Branch, but can do even more than either