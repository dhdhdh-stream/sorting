/**
 * - only update if successfully reached ending pattern from start pattern
 * 
 * - score function based on state instead of obs
 *   - after explore, localize, then examine
 */

#ifndef STATE_H
#define STATE_H



#endif /* STATE_H */